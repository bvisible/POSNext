[project]
name = "pos_next"
authors = [
    { name = "BrainWise", email = "support@brainwise.me"}
]
description = "“POS built on ERPNext that brings together real-time billing, stock management, multi-user access, offline mode, and direct ERP integration. Run your store or restaurant with confidence and control, while staying 100% open source."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=15.0.0" # Installed and managed by bench.
    # //// Neoffice — upstream declares only "erpnext" (no trailing comma). weasyprint added by
    # //// b6e757dd (2026-03-26, "add menu PDF generator with badges") to render the restaurant menu
    # //// PDF; fb475986 (2026-03-31) then dropped a pydyf pin that conflicted and relaxed the
    # //// minimum to >=59.0. NOTE for the next merge: pos_next/api/menu_pdf.py now renders through
    # //// wkhtmltopdf, not WeasyPrint (WeasyPrint OOM-crashes on small instances), so this
    # //// dependency is a candidate for removal — check menu_pdf.py before dropping it.
    "erpnext",
    "weasyprint>=59.0",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
