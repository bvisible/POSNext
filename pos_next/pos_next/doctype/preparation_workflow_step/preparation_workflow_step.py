#//// Neoffice — added file (no upstream equivalent). One step of a preparation workflow (name,
#//// colour, whether the KDS may still edit at that point) (d59036f1, 2026-03-23 "preparation
#//// workflows, runner display, and workflow APIs").
from frappe.model.document import Document


class PreparationWorkflowStep(Document):
	pass
