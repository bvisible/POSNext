{
  "name": "pos_next",
  "version": "1.15.0",
  "description": "POS built on ERPNext that brings together real-time billing, stock management, multi-user access, offline mode, and direct ERP integration. Run your store or restaurant with confidence and control, while staying 100% open source.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd POS && yarn install",
    "dev": "cd POS && yarn dev",
    "build": "if [ -d 'pos_next/public/pos/assets' ] && [ -z \"$FORCE_REBUILD\" ]; then echo '[skip-build] pre-built artifacts present at pos_next/public/pos/assets (set FORCE_REBUILD=1 to force rebuild)'; else yarn build:force; fi",
    "build:force": "cd POS && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dompurify": "^3.3.0",
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.9.14"
  }
}
